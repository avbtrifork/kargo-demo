apiVersion: apps/v1
kind: Deployment
metadata:
  name: kargo-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kargo-demo
  template:
    metadata:
      labels:
        app: kargo-demo
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: nginx
          image: ghcr.io/avbtrifork/kargo-demo-service1:tag_version
          imagePullPolicy: Always
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"
          # volumeMounts:

          
          # Adding a new change here to demonstrate a change detection
          #   - name: content
          #     mountPath: /usr/share/nginx/html
          #     readOnly: true
      # volumes:
      #   - name: content
      #     configMap:
      #       name: kargo-demo-content
